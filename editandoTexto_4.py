# Recopilamos una frase utilizando la función input y luego mostramos su valor en minúsculas 
# utilizando el método lower. Podemos mostrar la salida de esto en un comando print.

frase = input('Ingrese una frase en mayuscula: ')

print(frase.lower())
